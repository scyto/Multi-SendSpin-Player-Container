# Changelog - Development Builds

Development builds are automatically created from the `dev` branch.
These builds may contain unstable features and breaking changes.

For stable releases, use the main Multi-Room Audio add-on.
