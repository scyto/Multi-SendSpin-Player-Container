version: '3.8'

# Docker Compose configuration for environments without audio devices
# Usage: docker-compose -f docker-compose.no-audio.yml up

services:
  multiroom-audio:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: multiroom-audio-no-audio
    restart: unless-stopped
    ports:
      - "8096:8096"
    # No audio devices mapped - container will use virtual/null devices
    # devices:
      # FTDI USB Relay Board (optional - for 12V trigger control)
      # Uncomment the following to enable FTDI relay board support:
      # - /dev/bus/usb:/dev/bus/usb
    # cap_add:
      # FTDI USB Relay Board (optional - for 12V trigger control)
      # Uncomment the following to allow libftdi to detach kernel drivers:
      # - SYS_RAWIO
    volumes:
      # Persistent configuration storage
      - ./config:/app/config
      - ./logs:/app/logs
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    # Use bridge networking for better compatibility in no-audio environments
    # network_mode: bridge  # Uncomment if host networking causes issues
    privileged: false

    # Health check
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8096/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

# Optional: Create named volumes for persistence
volumes:
  multiroom_config:
    driver: local
  multiroom_logs:
    driver: local
